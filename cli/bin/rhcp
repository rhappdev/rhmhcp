#!/usr/bin/env node

const prom = require("commander");

prom.version(require("../package.json").version);
prom.command("init", "Initialise hot code push for the project in the current directory");
prom.command("publish","Publish current project.");

prom.parse(process.argv);

